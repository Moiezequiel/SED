import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
// Renderizar el componente Registro en el elemento raíz del DOM
ReactDOM.render(
  <React.StrictMode>
   <App/>
  </React.StrictMode>,
  document.getElementById('root')
);
